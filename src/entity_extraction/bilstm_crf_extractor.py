from typing import List
from src.utils.dataclasses import Sentence, EntityMention


def extract_entities(sentences: List[Sentence], config: dict):

    mentions = []

    # Placeholder extractor until model training is added
    # This keeps the pipeline stable while we integrate ML

    return mentions
